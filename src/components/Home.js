import React from 'react';
import Carousel from './Carousel';

const Home = ({children}) => {
    return(
        <div>
            <h1>PAGINA PRINCIPAL</h1>
            <Carousel/>
        </div>
    );
};

export default Home;
